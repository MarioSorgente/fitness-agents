# Admin Pages

The v1 admin submission pages live at:

- `/admin/submissions`
- `/admin/submissions/[id]`

## Security TODO

Mario requested no auth/RBAC for v1, so these pages are currently placeholders without roles,
sign-in checks, or access control. Before sharing the admin URL publicly or connecting real client
submission data, protect the admin area with authentication, authorization, and role-based access
control.
